messages:
  my_greeting:
    template: "Hello {{ .Name }}! You have {{ .Count }} messages."
    variables:
      Name: string
      Count: int
    custom_templates:
      "count == 1":  "Hello {{ .Name }}! You have {{ .Count }} message."
      "count == 0":  "Hello {{ .Name }}! You have no messages."
