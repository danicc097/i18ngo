messages:
  my_greeting:
    template: "Hola {{ .Name }}! Tienes {{ .Count }} mensajes."
    variables:
      Name: string
      Count: int
    custom_templates:
      "count == 1": "Hola {{ .Name }}! Tienes {{ .Count }} mensaje."
      "count == 0": "Hola {{ .Name }}! No tienes ningún mensaje."
